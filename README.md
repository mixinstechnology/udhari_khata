# Udhari Khata UI (minimal scaffold)

Commands:

```bash
npm install
npm run dev
```

Notes:
- API base URL set in `.env` as `VITE_API_BASE_URL`
- Login posts to `/api/users/login` and stores `token` and `user` in `localStorage`
- Sidebar contains Dashboard, Master->Party, Transaction->Add, Report->Dues Report
